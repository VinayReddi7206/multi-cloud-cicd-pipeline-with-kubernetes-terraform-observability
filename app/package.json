{
  "name": "multicloud-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Observable sample API for AKS and EKS",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test --experimental-test-coverage"
  }
}
